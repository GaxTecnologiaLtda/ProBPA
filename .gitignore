# Ignore everything in ROOT (non-recursive)
/*

# Whitelist specific project directories (and their contents)
!/functions/
!/connector_app/
!/src/
!/pacote_transferencia_windows/

# Whitelist critical infrastructure
!/.gitignore
!/.github/
!/firebase.json
!/firestore.rules
!/storage.rules
!/database.rules.json
!/package.json
!/package-lock.json
!/tsconfig.json
!/vite.config.ts

# Ignore system files even in whitelisted dirs (just in case)
.DS_Store
node_modules/
.env
.env.local
dist/
build/
.firebase/
